{
  "name": "gentech-ai",
  "version": "0.1.0",
  "private": true,
  "description": "GenTech AI — Vision Intelligence Orchestration Platform (agentic layer)",
  "type": "module",
  "scripts": {
    "build": "tsc --build",
    "typecheck": "tsc --build --force",
    "test": "vitest run",
    "test:watch": "vitest",
    "orchestrate": "tsx packages/orchestrator/src/cli.ts"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  },
  "engines": {
    "node": ">=20"
  }
}
